import React, { useEffect } from "react";

const Stream = () => {
  //   useEffect(() => {
  //     peer.on("call", (call) => {});
  //   }, []);

  return (
    <div>
      <button className="border-2 px-3" onClick={() => {}}>
        Start Stream
      </button>
    </div>
  );
};

export default Stream;
